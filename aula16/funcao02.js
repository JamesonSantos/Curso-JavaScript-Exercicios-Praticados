function soma(n1=0, n2=0) { //Se n1 ou n2 não forem passados, recebem valor 0
    return n1 + n2 //Retorna a soma de ambos
}

console.log(soma(2, 5)) // 2 passa para p primeiro parãmetro n1
                        // 5 passa para o segundo parãmetro n2
